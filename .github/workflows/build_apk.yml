name: Build Android APK

on:
  push:
    branches: [ main, 'feat/**' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./flutter_app

    steps:
    # 1. 코드 체크아웃
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. Android SDK 설정
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        cmdline-tools-version: 11076708
        packages: 'platforms;android-34 build-tools;34.0.0 platform-tools'

    # 3. Java 17 설정 (Gradle 8.2 필수)
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    # 4. Flutter 설정
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.22.x'
        channel: 'stable'
        cache: true

    # 5. Android Licenses
    - name: Accept Android Licenses
      run: yes | flutter doctor --android-licenses || true

    # ▼▼▼ [핵심] 6. Gradle 캐시 강제 삭제 (골든존!) ▼▼▼
    - name: Force Clean Gradle Cache
      run: |
        echo "🧹 Cleaning Gradle Cache..."
        rm -rf ~/.gradle/caches
        rm -rf ~/.gradle/wrapper
        echo "✅ Done."

    # [추가] 7-1. Gradle 8.2 설치 (Wrapper 없을 때 대비)
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: 8.2
    
    # 8. 라이브러리 다운로드
    - name: Get Flutter Dependencies
      run: flutter pub get

    # [Step 1] 준비 운동: 라이브러리만 먼저 다운로드 (빌드 X)
    # --refresh-dependencies: 캐시 무시하고 새로 받음
    # --max-workers=1: 무조건 일꾼 1명이 차근차근 다운로드 (충돌 0%)
    - name: Pre-load Dependencies
      run: |
        cd android
        # Wrapper 파일이 없으므로 시스템에 설치된 gradle을 직접 호출합니다.
        gradle dependencies --no-daemon --refresh-dependencies --max-workers=1
        cd ..

    # [Step 2] 본 게임: 이미 받아둔 파일로 APK 빌드
    # --offline을 쓰면 좋겠지만, Flutter가 내부적으로 pub get을 할 수 있으므로 일반 빌드로 하되,
    # 이미 Gradle 캐시가 완벽하므로 네트워크 충돌이 안 일어납니다.
    - name: Build APK
      env:
         GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.parallel=false -Dorg.gradle.workers.max=1"
      run: flutter build apk --release

    # 9. APK 업로드
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: solo-runner-native
        path: flutter_app/build/app/outputs/flutter-apk/app-release.apk
